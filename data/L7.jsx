/* Lecture 7: File-System Interface & Implementation */
window.LECTURES = window.LECTURES || {};
window.LECTURES[7] = {
  id: 7,
  title: "File-System Interface & Implementation",
  titleRu: "Интерфейс и реализация файловой системы",
  topics: ["File concept & attributes", "File operations", "Access methods", "Directory structures", "Mounting & sharing", "Protection (ACL)", "FS structure (layers)", "Directory implementation", "Allocation methods", "Free-space management"],
  summary: [
    "A **file** is a named collection of related information stored on secondary storage. The OS sees it as a contiguous logical address space. Files have **content** (bytes) and **metadata** (attributes).",
    "## File attributes",
    "• **Name** — human-readable (the only attribute kept in this form).",
    "• **Identifier** — unique tag (e.g. inode number).",
    "• **Type** — for systems that distinguish types.",
    "• **Location** — pointer to data on the device.",
    "• **Size** — current bytes.",
    "• **Protection** — who can read/write/execute.",
    "• **Timestamps** — creation, modification, access.",
    "• **Owner** — user and group.",
    "Stored in the **directory structure** on disk.",
    "## File operations",
    "**Create, read, write, seek (reposition), delete, truncate, open, close.** `open()` loads the directory entry into memory and returns a **file descriptor / handle**; `close()` releases it.",
    "## Open-file tables",
    "• **Per-process** — current position (read/write pointer), access mode, locks.",
    "• **System-wide** — location on disk, current size, reference count.",
    "## Access methods",
    "• **Sequential** — read/write next; like a tape. Default for many libraries.",
    "• **Direct (random)** — read/write block N by number; like a disk. Required for databases.",
    "• **Indexed** — index file points to data blocks; search the index first, fetch directly.",
    "## Directory structures",
    "• **Single-level** — one flat directory. Name collisions; no grouping.",
    "• **Two-level** — master directory + user file directory per user. Solves name collisions.",
    "• **Tree** — subdirectories within subdirectories. Absolute / relative paths; cwd.",
    "• **Acyclic graph** — sharing via **hard / symbolic links**. Same file reachable by multiple paths. Issues: deletion semantics, reference counts.",
    "• **General graph** — cycles allowed. Needs garbage collection.",
    "## Mounting",
    "Splices a file system at a **mount point** (existing directory). Consistency check at mount time. Records in the **mount table**. Cascading mounts allowed.",
    "## File sharing — consistency semantics",
    "When are writes by one user visible to others?",
    "• **UNIX semantics** — immediately.",
    "• **Session semantics** — only after close.",
    "• **Immutable-shared-files** — content fixed once shared.",
    "## Protection",
    "• **ACLs** — per-file list of (user, allowed ops). Powerful, bulky. NTFS, POSIX ACLs.",
    "• **UNIX mode bits** — 9 bits = (owner/group/other) × (rwx) + setuid/setgid/sticky.",
    "• **Role-based** — users have roles, roles have permissions.",
    "## FS layered architecture (bottom → top)",
    "• **I/O control** — device drivers, interrupt handlers.",
    "• **Basic FS** — issues generic read/write for physical blocks; manages buffer cache.",
    "• **File-organization module** — translates logical → physical block addresses; free-block management.",
    "• **Logical FS** — metadata (FCB / inode), directory structure, protection; syscall interface.",
    "## Directory implementation",
    "• **Linear list** — simple, O(n) lookup.",
    "• **Hash table** — O(1) average; fixed size; collision handling.",
    "• **B+-tree** — scales to millions of entries (NTFS, ext4 htree).",
    "## Allocation methods",
    "• **Contiguous** — file in consecutive blocks (start, length). Fast random access; external fragmentation; growth issues.",
    "• **Linked** — each block points to next. No external fragmentation; O(n) random access; corruption loses tail. **FAT** stores pointers in a table → speeds up random access.",
    "• **Indexed** — per-file **index block** lists all data-block addresses. O(1) random access; pays one block of overhead. Large files: multi-level / linked indexes. UNIX **inode**: direct + single/double/triple indirect blocks.",
    "## Free-space management",
    "• **Bit vector** — 1 bit per block (0 used, 1 free). Compact; fast bitwise scans.",
    "• **Linked list** — link free blocks together. Cheap on memory; traversal expensive.",
    "• **Grouping** — first free block stores N free addresses; chain to next group.",
    "• **Counting** — entries are (start, run length). Compact when allocations are large.",
    "## Performance",
    "Caching is decisive: **buffer cache** (file data), **page cache** (mmap'd files); often merged. **Read-ahead** and **write-behind** smooth I/O. Synchronous writes hurt performance but improve durability.",
    "## Recovery",
    "• **Consistency check** — fsck / chkdsk examines metadata after a crash and tries to repair.",
    "• **Journaling** (ext4, NTFS, ZFS) — write a journal entry first, do the op, mark journal done; on crash replay or undo."
  ],
  summaryRu: [
    "**Файл** — именованный набор связанных данных на вторичном хранилище. ОС видит его как непрерывное логическое адресное пространство. У файла **содержимое** (байты) и **метаданные** (атрибуты).",
    "## Атрибуты файла",
    "• **Имя** — читаемое (единственное в таком виде).",
    "• **Идентификатор** — уникальный (inode).",
    "• **Тип** — для систем, различающих типы.",
    "• **Расположение** — указатель на данные.",
    "• **Размер** — текущие байты.",
    "• **Защита** — кто может R/W/X.",
    "• **Временные метки** — создание, модификация, доступ.",
    "• **Владелец** — user и group.",
    "Хранится в **структуре каталога** на диске.",
    "## Операции",
    "**Create, read, write, seek, delete, truncate, open, close.** `open()` загружает запись каталога в память и возвращает **дескриптор/handle**; `close()` освобождает.",
    "## Таблицы открытых файлов",
    "• **Per-process** — позиция чтения/записи, режим, локи.",
    "• **Общесистемная** — расположение на диске, размер, счётчик ссылок.",
    "## Методы доступа",
    "• **Последовательный** — read/write next; как лента.",
    "• **Прямой (случайный)** — read/write блок N по номеру; как диск. Нужен БД.",
    "• **Индексный** — индексный файл указывает на блоки; поиск по индексу, прямое чтение.",
    "## Структуры каталогов",
    "• **Одноуровневый** — плоский каталог. Коллизии имён; нет группировки.",
    "• **Двухуровневый** — master + per-user. Решает коллизии.",
    "• **Дерево** — подкаталоги в подкаталогах. Абсолютные/относительные пути; cwd.",
    "• **Ациклический граф** — общий доступ через **жёсткие/символические ссылки**. Один файл — несколько путей. Проблемы: семантика удаления, счётчики ссылок.",
    "• **Общий граф** — допускает циклы. Нужен сборщик мусора.",
    "## Монтирование",
    "Вставляет ФС в **точку монтирования** (существующий каталог). Проверка консистентности. Запись в **mount table**. Каскадные монтирования.",
    "## Совместный доступ — семантика консистентности",
    "Когда записи одного видны другим?",
    "• **UNIX** — сразу.",
    "• **Session** — только после close.",
    "• **Immutable-shared** — содержимое неизменно.",
    "## Защита",
    "• **ACL** — per-file список (user, операции). Мощно, громоздко. NTFS, POSIX ACL.",
    "• **UNIX mode bits** — 9 бит = (owner/group/other) × (rwx) + setuid/setgid/sticky.",
    "• **Ролевая** — у пользователя роли, у ролей права.",
    "## Слоистая архитектура ФС (снизу → вверх)",
    "• **I/O control** — драйверы, обработчики прерываний.",
    "• **Basic FS** — общие команды read/write для физических блоков; buffer cache.",
    "• **File-organization module** — логические → физические блоки; управление свободными блоками.",
    "• **Logical FS** — метаданные (FCB / inode), каталоги, защита; syscall-интерфейс.",
    "## Реализация каталога",
    "• **Линейный список** — просто, O(n).",
    "• **Хеш-таблица** — O(1) в среднем; фикс. размер; коллизии.",
    "• **B+-дерево** — миллионы записей (NTFS, ext4 htree).",
    "## Методы размещения",
    "• **Непрерывное** — файл в подряд идущих блоках (start, length). Быстрый случайный доступ; внешняя фрагментация; рост труден.",
    "• **Связное** — каждый блок указывает на следующий. Нет внешней фрагментации; O(n) случайный доступ; повреждение теряет хвост. **FAT** хранит указатели в таблице → ускоряет случайный доступ.",
    "• **Индексное** — per-file **индексный блок** со всеми адресами. O(1) случайный доступ; накладной 1 блок. Большие файлы — многоуровневые/связные индексы. UNIX **inode**: direct + single/double/triple indirect.",
    "## Управление свободным местом",
    "• **Битовый вектор** — 1 бит на блок (0 занят, 1 свободен). Компактно; быстрые битовые сканы.",
    "• **Связный список** — связываем свободные блоки. Экономно по памяти; обход дорог.",
    "• **Группировка** — первый свободный блок хранит N свободных адресов; цепочка к следующей группе.",
    "• **Счёт** — записи (start, run length). Компактно при больших выделениях.",
    "## Производительность",
    "Кэширование решает всё: **buffer cache** (данные файлов), **page cache** (mmap-файлы); часто объединены. **Read-ahead** и **write-behind** сглаживают I/O. Синхронные записи бьют по скорости, но повышают надёжность.",
    "## Восстановление",
    "• **Проверка консистентности** — fsck / chkdsk анализирует метаданные после краха и пытается починить.",
    "• **Журналирование** (ext4, NTFS, ZFS) — сначала запись в журнал, потом операция, потом отметка о завершении; при крахе журнал воспроизводится или откатывается."
  ],
  flashcards: [
    {q: "Define a file in OS terms.", a: "A named collection of related information stored on a device, viewed by the OS as a contiguous logical address space. Has metadata (attributes) and content.", ru: "Именованный набор связанных данных на устройстве; ОС видит его как непрерывное логическое адресное пространство. Имеет метаданные и содержимое.", diff: "easy"},
    {q: "What is a file descriptor / handle?", a: "A small integer (UNIX) or opaque token (Windows) returned by open() that the OS uses to index into the process's open-file table. Reads/writes use it instead of the file's path.", ru: "Маленькое целое (UNIX) или непрозрачный токен (Windows), который возвращает open() и через который ОС индексирует таблицу открытых файлов процесса. Чтение/запись используют его, а не путь.", diff: "easy"},
    {q: "Sequential vs direct access — when does each shine?", a: "Sequential: best for tape and bulk read-through (logs, streams). Direct/random: best for disks and databases where you need block N immediately by number.", ru: "Последовательный — для лент и потокового чтения (логи, потоки). Прямой/случайный — для дисков и БД, где нужен блок N сразу.", diff: "easy"},
    {q: "Acyclic-graph directory — what problem does it solve and what new ones appear?", a: "Solves: lets users share a file or subtree (UNIX hard/symbolic links). New: deciding when to actually delete a shared file (reference counting), avoiding accidental cycles.", ru: "Решает: общий доступ к файлу или поддереву (жёсткие/символические ссылки UNIX). Создаёт: когда удалять общий файл (подсчёт ссылок), как избежать циклов.", diff: "hard"},
    {q: "What does it mean to 'mount' a file system?", a: "To attach a separate file system at a mount point — an existing empty directory. From then on, accessing that directory transparently accesses the mounted FS's root.", ru: "Подключить отдельную ФС к точке монтирования — пустому каталогу. После этого обращение к каталогу прозрачно идёт в корень смонтированной ФС.", diff: "medium"},
    {q: "UNIX permission bits — what do they mean?", a: "9 bits, three groups of three (rwx) for owner / group / other. r = read, w = write, x = execute (or 'enter' for a directory). Plus extra bits: setuid, setgid, sticky.", ru: "9 бит, три тройки (rwx) для owner / group / other. r — чтение, w — запись, x — исполнение (или «вход» в каталог). Плюс setuid, setgid, sticky.", diff: "easy"},
    {q: "Contiguous allocation — pros and cons.", a: "Pros: fast sequential AND random access (compute block# from start + offset). Simple. Cons: external fragmentation; growing a file is hard — needs known max size or relocation.", ru: "Плюсы: быстрый последовательный И случайный доступ (номер блока = start + offset). Просто. Минусы: внешняя фрагментация; рост файла затруднён — нужен заранее размер или перемещение.", diff: "medium"},
    {q: "Linked allocation — pros and cons.", a: "Each block stores a pointer to the next. Pros: no external fragmentation; files grow freely. Cons: random access expensive (must follow chain), pointer overhead per block, a corrupt pointer loses the rest of the file.", ru: "Каждый блок хранит указатель на следующий. Плюсы: нет внешней фрагментации; свободный рост. Минусы: случайный доступ дорог (надо идти по цепи), накладные на указатели, повреждение указателя теряет хвост.", diff: "medium"},
    {q: "Indexed allocation — main idea.", a: "Each file has an INDEX BLOCK listing the addresses of all its data blocks. To access block i you look up index[i]. Supports random access, no external fragmentation, but every file pays one index block of overhead — and large files may need multi-level indexes.", ru: "У каждого файла — БЛОК ИНДЕКСОВ со списком адресов всех его блоков данных. Доступ к блоку i = index[i]. Случайный доступ, нет внешней фрагментации; накладные на индексный блок; большим файлам нужен многоуровневый индекс.", diff: "medium"},
    {q: "Linear list vs hash table directory implementation.", a: "Linear: simple, but every lookup may need to scan all entries (O(n)). Hash table: O(1) expected lookups, but needs collision handling and may need rebuild when full. B+-trees scale to very large dirs.", ru: "Список: просто, но поиск O(n). Хеш: O(1) в среднем, но коллизии и фиксированный размер. B+-деревья — для очень больших каталогов.", diff: "medium"},
    {q: "Bit-vector free-space management — pros and cons.", a: "1 bit per block: 0 = used, 1 = free. Pros: very compact, fast to find first free block with bitwise ops. Cons: scanning the whole vector is slow on huge disks; must keep in memory for speed.", ru: "1 бит на блок: 0 — занят, 1 — свободен. Плюсы: компактно, быстро находить первый свободный битовыми операциями. Минусы: полный скан медленный на больших дисках; держать в памяти.", diff: "medium"},
    {q: "Three layers in a typical FS implementation (bottom-up).", a: "I/O control layer (drivers and interrupt handlers) → basic file system (read/write whole physical blocks) → file-organization module (translates logical → physical block addresses) → logical file system (metadata, FCB/inode, directories, protection).", ru: "I/O control (драйверы, обработчики прерываний) → basic FS (чтение/запись физических блоков) → file-organization module (логические → физические блоки) → logical FS (метаданные, FCB/inode, каталоги, защита).", diff: "hard"}
  ],
  quiz: [
    {q: "Which allocation method gives O(1) random access but suffers external fragmentation?", opts: ["Linked", "Indexed", "Contiguous", "Hashed"], correct: 2, expl: "Contiguous allocation: block# = start + offset is O(1), but free space gets fragmented as files come and go.", explRu: "Непрерывное: номер блока = start + offset — O(1), но свободное место фрагментируется."},
    {q: "Linked allocation makes random access expensive because:", opts: ["The OS encrypts each block", "You must follow the chain block-by-block to reach a target", "Linked files can't be seeked", "It needs an MMU"], correct: 1, expl: "To reach block N you must traverse blocks 0..N−1 following pointers. Random access is therefore O(N).", explRu: "Чтобы добраться до блока N, нужно пройти 0..N−1 по указателям. Случайный доступ — O(N)."},
    {q: "Indexed allocation pays which fixed overhead per file?", opts: ["A full copy of the directory", "One index block listing data-block addresses", "A backup chain", "Encryption keys"], correct: 1, expl: "Each file gets its own index block. Big files need multi-level/linked indexes.", explRu: "У каждого файла — индексный блок. Большие файлы используют многоуровневые/связные индексы."},
    {q: "UNIX permission '755' means:", opts: ["Owner rwx, group r-x, other r-x", "Owner rw-, group rwx, other rwx", "Owner rwx, group rwx, other r--", "Owner ---, group rwx, other rwx"], correct: 0, expl: "Each digit is the octal of one triple. 7 = rwx, 5 = r-x. So 755 = rwxr-xr-x.", explRu: "Каждая цифра — восьмеричная запись тройки. 7=rwx, 5=r-x. 755 = rwxr-xr-x."},
    {q: "Which directory structure naturally lets two users share a single file?", opts: ["Single-level", "Two-level (per user)", "Tree", "Acyclic graph (with links)"], correct: 3, expl: "An acyclic graph permits multiple paths to a file via links — UNIX's hard and symbolic links.", explRu: "Ациклический граф позволяет несколько путей к файлу через ссылки — жёсткие и символические в UNIX."},
    {q: "A hashed-directory implementation primarily improves:", opts: ["Storage efficiency", "Lookup time compared with linear scan", "Block allocation", "Free-space tracking"], correct: 1, expl: "Hashing turns O(n) linear lookups into ~O(1) average. The trade-off is collision handling and a fixed table size.", explRu: "Хеш превращает O(n) поиск в среднем O(1). Минус — коллизии и фиксированный размер."},
    {q: "What does mounting do?", opts: ["Permanently formats the disk", "Attaches a file system at an existing directory so it appears as a sub-tree", "Encrypts file contents", "Reserves disk space for the kernel"], correct: 1, expl: "Mounting splices a separate FS into the existing namespace at a chosen mount point.", explRu: "Монтирование вставляет отдельную ФС в существующее пространство имён в точке монтирования."},
    {q: "A bit vector for free-space management is most efficient when:", opts: ["The disk is huge and the vector can't fit in memory", "The disk is small enough that the bitmap fits in RAM and bitwise scans are fast", "Files are encrypted", "The FS uses linked allocation"], correct: 1, expl: "Bit vectors excel when the bitmap fits in memory; bitwise ops then find free blocks very quickly.", explRu: "Битовый вектор эффективен, если помещается в память — битовые операции быстро находят свободное."}
  ],
  traps: [
    {t: "Indexed allocation handles random access well — better than linked. Don't confuse it with linked just because both 'aren't contiguous'.", ru: "Индексное размещение хорошо обрабатывает случайный доступ — лучше связного. Не путать только потому что оба «не непрерывные»."},
    {t: "A symbolic link can dangle (point to a deleted target). A hard link cannot — every hard link increments the inode's link count, and the file is freed only when the count hits zero.", ru: "Символическая ссылка может «висеть» (цель удалена). Жёсткая — нет: каждая жёсткая ссылка увеличивает счётчик ссылок inode; файл освобождается только при счётчике 0."},
    {t: "Mounting doesn't change disk contents — it's purely in-memory bookkeeping. Unmounting just detaches it; the underlying data persists.", ru: "Монтирование не меняет содержимое диска — это чистое внутрипамятное состояние. Размонтирование лишь отсоединяет; данные остаются."}
  ]
};
