/* Lecture 8: File-System Internals & Windows 10 */
window.LECTURES = window.LECTURES || {};
window.LECTURES[8] = {
  id: 8,
  title: "File-System Internals & Windows 10",
  titleRu: "Внутренности ФС и Windows 10",
  topics: ["Partitions & mounting", "Virtual File Systems (VFS)", "Remote file systems (NFS)", "NFS mount/protocol/architecture", "Windows 10 design goals", "Windows architecture (HAL, kernel, executive)", "Memory management on Windows"],
  summary: [
    "Lecture 8 covers two big topics: how file systems are implemented underneath (partitions, VFS, NFS), and Windows 10's architecture in detail.",
    "## Partitions and mounting",
    "• **Partition** — a **volume** with a file system ('cooked'), or **raw** (just blocks — used by swap, raw databases, RAID).",
    "• **Boot block** points to a boot volume or boot loader.",
    "• **Root partition** holds the OS, mounted at boot.",
    "• Other partitions auto-mount or mount manually.",
    "• At mount time, FS **consistency is checked**; if not OK, the FS tries to repair. Records go in the **mount table**.",
    "## Virtual File System (VFS)",
    "Object-oriented interface so one set of syscalls (open/read/write/close) works across many FS types — local (ext4, NTFS), remote (NFS), virtual (procfs, sysfs).",
    "## VFS — four primary objects (Linux)",
    "• **inode** — a single file (metadata: type, perms, owner, size, data-block pointers).",
    "• **file** — an open-file instance (read/write pointer, mode, link to inode).",
    "• **superblock** — a whole mounted FS (block size, free counts, root inode).",
    "• **dentry** — a directory entry (name + pointer to inode); cached for fast path resolution.",
    "Each object has a **function table** dispatching to the actual FS implementation.",
    "## NFS — Network File System",
    "Mount a remote directory and access it as if local. Works across heterogeneous machines via **RPC** over UDP/TCP + **XDR** (cross-platform data encoding).",
    "## NFS mount protocol",
    "1. Client requests mount of a remote dir.",
    "2. Server checks its **export list** (which FSes may be mounted by whom).",
    "3. If allowed, returns a **file handle** = (FS id, inode number) — used as a key for further requests.",
    "4. The mount changes only the CLIENT's namespace; server is untouched.",
    "## NFS — stateless servers (v2/v3)",
    "Each request carries all info needed. Server crash recovery is trivial — client just retries idempotent calls. Trade-off: modified data must be **committed to disk** before the server returns (synchronous writes) — gives up caching wins. NFS provides no built-in concurrency control. v4 is stateful.",
    "## NFS architecture — three layers",
    "• **UNIX FS interface** — open/read/write/close + file descriptors.",
    "• **VFS layer** — distinguishes local vs remote; dispatches.",
    "• **NFS service** — the wire protocol over RPC.",
    "NFS performance: **file-blocks cache** + **file-attribute cache** on the client, with revalidation rules.",
    "## Windows 10 — design goals",
    "• **Portability** — written in C/C++; hardware-specific code in the HAL.",
    "• **Security**.",
    "• **POSIX compliance** (historically; now WSL).",
    "• **Multiprocessor support** — designed for SMP.",
    "• **Extensibility** — modular subsystems.",
    "• **International support** — NLS API.",
    "• **Compatibility** — with MS-DOS and Win apps.",
    "Architecturally a **hybrid kernel** (microkernel ideas + executive in kernel mode).",
    "## Windows architecture — protected mode vs user mode",
    "**Protected mode**:",
    "• **HAL** (Hardware Abstraction Layer) — DLL hiding CPU/chipset specifics.",
    "• **Kernel** — thread scheduling, interrupts, low-level sync, power recovery. Never paged, never preempted.",
    "• **Executive** — system services on top of the kernel.",
    "**User mode**:",
    "• **Environmental subsystems** — emulate other OS APIs (Win32, historically POSIX, OS/2).",
    "• **Protection subsystems** — security services.",
    "## Windows Executive — major components",
    "• **Object Manager** — manages kernel objects (processes, threads, files, events); naming, handles, security checks.",
    "• **Process Manager** — creates/deletes processes and threads.",
    "• **Virtual Memory Manager** — paging, working sets, shared memory.",
    "• **Security Reference Monitor** — enforces access control.",
    "• **I/O Manager** — dispatches I/O to drivers; manages FSes.",
    "• **Cache Manager** — FS cache.",
    "• **Window Manager (Win32k)** — GUI.",
    "## Windows memory management",
    "Paging (4 KB pages on x86). 32-bit virtual address space typically split 2 GB user + 2 GB kernel. **Demand paging** with clustering (read neighbouring pages too). Per-process **working sets** managed against a global free-page pool. Shared memory via **section objects**."
  ],
  summaryRu: [
    "Лекция 8 покрывает две большие темы: как ФС реализованы под капотом (разделы, VFS, NFS) и архитектуру Windows 10 в деталях.",
    "## Разделы и монтирование",
    "• **Раздел** — **том** с файловой системой ('cooked') или **сырой** (просто блоки — для swap, raw БД, RAID).",
    "• **Boot-блок** указывает на загрузочный том или загрузчик.",
    "• **Корневой раздел** содержит ОС, монтируется при загрузке.",
    "• Остальные разделы — авто-монтирование или вручную.",
    "• При монтировании **проверяется консистентность**; при ошибках ФС пробует починить. Запись идёт в **mount table**.",
    "## Virtual File System (VFS)",
    "Объектно-ориентированный интерфейс: один набор syscall (open/read/write/close) работает с разными ФС — локальными (ext4, NTFS), удалёнными (NFS), виртуальными (procfs, sysfs).",
    "## VFS — четыре основных объекта (Linux)",
    "• **inode** — один файл (метаданные: тип, права, владелец, размер, указатели блоков).",
    "• **file** — открытый файл (указатель чтения/записи, режим, ссылка на inode).",
    "• **superblock** — смонтированная ФС целиком (размер блока, счётчики свободного, корневой inode).",
    "• **dentry** — запись каталога (имя + указатель на inode); кэшируется для быстрой обработки путей.",
    "У каждого объекта **таблица функций**, диспетчирующая в реальную ФС.",
    "## NFS — Network File System",
    "Смонтировать удалённый каталог и работать с ним как с локальным. На разнородных машинах — через **RPC** поверх UDP/TCP + **XDR** (кросс-платформенное представление данных).",
    "## Протокол монтирования NFS",
    "1. Клиент запрашивает монтирование удалённого каталога.",
    "2. Сервер сверяется с **export list** (какие ФС можно монтировать и кому).",
    "3. Если разрешено — возвращает **file handle** = (id ФС, inode) — ключ для дальнейших обращений.",
    "4. Монтирование меняет только пространство имён КЛИЕНТА; сервер не меняется.",
    "## NFS — stateless серверы (v2/v3)",
    "Каждый запрос несёт всё нужное. Восстановление после краха тривиально — клиент повторяет идемпотентные вызовы. Цена: изменённые данные должны быть **закоммичены на диск** до возврата от сервера (синхронные записи) — теряются плюсы кэширования. NFS не имеет встроенного управления конкуренцией. v4 — stateful.",
    "## Архитектура NFS — три слоя",
    "• **UNIX FS interface** — open/read/write/close + file descriptors.",
    "• **Слой VFS** — различает локальное и удалённое; диспетчирует.",
    "• **Сервис NFS** — протокол по сети поверх RPC.",
    "Производительность NFS: **кэш блоков** + **кэш атрибутов** на клиенте, с правилами реваидации.",
    "## Windows 10 — цели дизайна",
    "• **Переносимость** — основной код C/C++; зависящий от железа — в HAL.",
    "• **Безопасность**.",
    "• **POSIX-совместимость** (исторически; сейчас WSL).",
    "• **Многопроцессорность** — рассчитана на SMP.",
    "• **Расширяемость** — модульные подсистемы.",
    "• **Интернационализация** — NLS API.",
    "• **Совместимость** — с MS-DOS и Win-приложениями.",
    "Архитектурно — **гибридное ядро** (идеи микроядра + executive в kernel mode).",
    "## Архитектура Windows — protected mode vs user mode",
    "**Protected mode**:",
    "• **HAL** (Hardware Abstraction Layer) — DLL, скрывающий CPU/чипсет-специфику.",
    "• **Kernel** — планирование потоков, прерывания, низкоуровневая sync, восстановление питания. Не выгружается, не вытесняется.",
    "• **Executive** — системные сервисы поверх ядра.",
    "**User mode**:",
    "• **Environmental subsystems** — эмулируют API других ОС (Win32, исторически POSIX, OS/2).",
    "• **Protection subsystems** — сервисы безопасности.",
    "## Windows Executive — основные компоненты",
    "• **Object Manager** — управляет объектами ядра (процессы, потоки, файлы, события); именование, handle, проверки безопасности.",
    "• **Process Manager** — создаёт/удаляет процессы и потоки.",
    "• **Virtual Memory Manager** — пейджинг, working sets, разделяемая память.",
    "• **Security Reference Monitor** — обеспечивает контроль доступа.",
    "• **I/O Manager** — диспетчирует I/O драйверам; управляет ФС.",
    "• **Cache Manager** — кэш ФС.",
    "• **Window Manager (Win32k)** — GUI.",
    "## Управление памятью Windows",
    "Пейджинг (4 КБ страницы на x86). 32-битное виртуальное пространство обычно делится 2 ГБ user + 2 ГБ kernel. **Demand paging** с кластеризацией (читает соседние страницы). На процесс — **working sets**, против глобального пула свободных страниц. Разделяемая память через **section objects**."
  ],
  flashcards: [
    {q: "What is a VFS (Virtual File System)?", a: "An abstraction layer that lets the kernel use one uniform interface (open, read, write, close) across many different file-system implementations — local, network, or virtual.", ru: "Уровень абстракции: ядро использует единый интерфейс (open, read, write, close) для разных ФС — локальных, сетевых или виртуальных.", diff: "easy"},
    {q: "Name the four core VFS objects in Linux.", a: "inode (file metadata), file (open-file instance with offset/flags), superblock (mounted FS as a whole), dentry (directory entry, one path component).", ru: "inode (метаданные файла), file (экземпляр открытого файла с offset/flags), superblock (смонтированная ФС), dentry (запись каталога).", diff: "medium"},
    {q: "Why is NFS designed to be stateless (v2/v3)?", a: "So a server crash and reboot are transparent: it has no client state to lose. Clients retry idempotent operations. Trade-off: every request must carry full context (no open/close).", ru: "Чтобы перезапуск сервера был прозрачен — нет клиентского состояния, которое можно потерять. Клиент повторяет идемпотентные запросы. Минус: каждый запрос несёт полный контекст.", diff: "hard"},
    {q: "What is an NFS file handle?", a: "A token returned by the server after a successful mount/lookup, identifying a specific file as (file-system identifier, inode number). The client uses it on every subsequent operation.", ru: "Токен, возвращаемый сервером после mount/lookup; идентифицирует файл как (id ФС, inode). Клиент использует его на каждом дальнейшем запросе.", diff: "medium"},
    {q: "Why does NFS rely on RPC and XDR?", a: "RPC makes remote calls look like local function calls. XDR (External Data Representation) standardizes byte order and data layout so heterogeneous machines (big/little endian, different word sizes) can interoperate.", ru: "RPC делает удалённые вызовы похожими на локальные. XDR — стандарт представления данных (порядок байтов, размеры), чтобы машины разной архитектуры могли общаться.", diff: "hard"},
    {q: "What does the NFS mount operation change?", a: "Only the CLIENT's view of the namespace. The server's filesystem is untouched. The client now sees the remote directory at its chosen mount point.", ru: "Только ПРЕДСТАВЛЕНИЕ КЛИЕНТА. Серверная ФС не меняется. Клиент теперь видит удалённый каталог в своей точке монтирования.", diff: "medium"},
    {q: "Windows 10 design goal: 'portability' — how is it achieved?", a: "Most of Windows is written in C/C++. Processor-specific code is isolated in the HAL (Hardware Abstraction Layer), a DLL. Porting to a new architecture mainly means porting the HAL.", ru: "Основной код — C/C++. Процессоро-зависимый код — в HAL (DLL). Перенос на новую архитектуру — в основном перенос HAL.", diff: "medium"},
    {q: "What's in the Windows Executive?", a: "Kernel-mode services: Object Manager, Process Manager, Virtual Memory Manager, Security Reference Monitor, I/O Manager, Cache Manager, plus the Window Manager (Win32k) for GUI.", ru: "Сервисы режима ядра: Object Manager, Process Manager, Virtual Memory Manager, Security Reference Monitor, I/O Manager, Cache Manager, Window Manager (Win32k).", diff: "hard"},
    {q: "Windows kernel — four primary responsibilities.", a: "Thread scheduling, interrupt and exception handling, low-level processor synchronization, recovery after a power failure. It is never paged out and execution is never preempted.", ru: "Планирование потоков, обработка прерываний и исключений, низкоуровневая синхронизация процессоров, восстановление после потери питания. Не выгружается и не вытесняется.", diff: "hard"},
    {q: "What is the HAL and why does Windows use one?", a: "Hardware Abstraction Layer — a DLL that hides CPU and chipset differences. The rest of the kernel calls HAL functions instead of touching hardware directly, simplifying ports between architectures.", ru: "Hardware Abstraction Layer — DLL, скрывающий различия CPU и чипсетов. Остальное ядро вызывает функции HAL, не трогая железо напрямую — упрощает переносы.", diff: "medium"},
    {q: "Environmental subsystems in Windows — purpose?", a: "User-mode personalities that emulate different OS APIs (Win32, originally POSIX and OS/2) so apps written for those environments run unchanged on the same Windows executive.", ru: "Пользовательские «личности», эмулирующие API разных ОС (Win32, ранее POSIX и OS/2) — приложения для них работают на одном и том же executive.", diff: "medium"},
    {q: "Is Windows monolithic or microkernel?", a: "A HYBRID. It uses microkernel concepts (separate executive + subsystems + HAL) but keeps the executive in kernel mode for performance — closer to monolithic in that respect.", ru: "ГИБРИДНОЕ. Использует идеи микроядра (отдельные executive, подсистемы, HAL), но executive в режиме ядра ради производительности — ближе к монолитному.", diff: "medium"}
  ],
  quiz: [
    {q: "VFS is BEST described as:", opts: ["A virtual machine for file systems", "An OO interface that abstracts over multiple FS implementations beneath one syscall API", "An NFS-only protocol layer", "A backup tool"], correct: 1, expl: "VFS exposes one set of file syscalls and dispatches to the correct FS implementation underneath.", explRu: "VFS даёт один набор системных вызовов и внутри диспетчирует в конкретную ФС."},
    {q: "An NFS v3 server is stateless because:", opts: ["It refuses connections", "Every client request carries enough info to be processed independently; server keeps no per-client state", "It runs entirely in RAM", "It never caches data"], correct: 1, expl: "Stateless = no per-client state on the server; survives crashes transparently.", explRu: "Stateless = нет клиентского состояния на сервере; крах прозрачен."},
    {q: "An NFS file handle identifies:", opts: ["A user's login session", "A specific file via (FS-id, inode-number)", "The mount point on the client", "The IP of the NFS server"], correct: 1, expl: "The handle is a stable cookie referring to one file across requests.", explRu: "Handle — стабильный токен, ссылающийся на файл между запросами."},
    {q: "The Hardware Abstraction Layer (HAL) in Windows is best described as:", opts: ["A device driver", "A DLL isolating CPU/chipset-specific code from the rest of the kernel", "Part of the file system", "A user-mode service"], correct: 1, expl: "HAL hides hardware specifics so the kernel and executive code are portable.", explRu: "HAL скрывает специфику железа, делая ядро и executive переносимыми."},
    {q: "Which of the following is NOT part of the Windows Executive?", opts: ["Object Manager", "Process Manager", "Security Reference Monitor", "GRUB bootloader"], correct: 3, expl: "GRUB is a Linux/Unix bootloader, not part of Windows. The Executive contains object/process/memory/security/I-O managers.", explRu: "GRUB — Linux/Unix-загрузчик, не часть Windows. Executive содержит менеджеры объектов/процессов/памяти/безопасности/I-O."},
    {q: "Which is the BEST description of Windows kernel design today?", opts: ["Pure monolithic", "Pure microkernel", "Hybrid kernel — microkernel concepts but executive runs in kernel mode", "Exokernel"], correct: 2, expl: "Windows uses microkernel-style modularity (HAL, kernel, executive, subsystems) but keeps the executive in kernel mode for speed — a hybrid.", explRu: "Windows — гибрид: модульность микроядра (HAL, kernel, executive, subsystems), но executive в режиме ядра ради скорости."},
    {q: "Environmental subsystems in Windows allowed Windows to:", opts: ["Run at higher CPU speeds", "Run apps written for other OS APIs (Win32, POSIX, OS/2) unchanged", "Skip booting", "Replace the file system"], correct: 1, expl: "They provide OS personalities so apps for different APIs can run on the same kernel.", explRu: "Они дают «личности» ОС, чтобы приложения для разных API работали на одном ядре."},
    {q: "VFS in NFS architecture sits between:", opts: ["The disk and the device driver", "The UNIX FS interface and the NFS service / local FS implementations", "Two NFS servers", "User space and the bootloader"], correct: 1, expl: "VFS routes calls from the syscall interface either to a local FS implementation or to the NFS client.", explRu: "VFS направляет вызовы syscall-интерфейса либо в локальную ФС, либо в клиент NFS."}
  ],
  traps: [
    {t: "NFS v2/v3 is stateless. NFS v4 is STATEFUL — it tracks opens, locks, etc. Don't generalize 'NFS is stateless' on a v4 question.", ru: "NFS v2/v3 — stateless. NFS v4 — stateful (отслеживает open, локи). Не обобщайте «NFS stateless» для v4."},
    {t: "Mounting a remote FS via NFS only changes the CLIENT'S view. Don't say it modifies the server's namespace.", ru: "Монтирование NFS меняет только ВЗГЛЯД КЛИЕНТА. Не пишите, что меняется пространство имён сервера."},
    {t: "Windows is NOT a pure microkernel. The executive runs in kernel mode for performance — calling Windows a microkernel without 'hybrid' is wrong.", ru: "Windows — НЕ чистое микроядро. Executive в режиме ядра ради скорости. Назвать Windows «микроядром» без слова «гибрид» — ошибка."}
  ]
};
