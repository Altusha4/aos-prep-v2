/* Lecture 4: Threads & Concurrency */
window.LECTURES = window.LECTURES || {};
window.LECTURES[4] = {
  id: 4,
  title: "Threads & Concurrency",
  titleRu: "Потоки и параллелизм",
  topics: ["Threads vs processes", "Benefits", "Concurrency vs parallelism", "Multithreading models", "Thread libraries", "Implicit threading", "Issues: fork/signals/cancellation"],
  summary: [
    "A **thread** is the basic unit of CPU utilization. Each has its own thread ID, program counter, registers, and stack. Threads in one process SHARE its code, data, heap, and OS resources (open files, signals, address space). This sharing is both their power and their danger.",
    "## Why we need threads",
    "A single-threaded process does one thing at a time. A word processor with one thread freezes the UI during spell-check. Servers (web, database) handle requests with a thread per request (or a thread pool) to serve many clients in parallel.",
    "## Four benefits of multithreading",
    "• **Responsiveness** — UI stays interactive while background work runs.",
    "• **Resource sharing** — threads share process memory by default; much cheaper than IPC.",
    "• **Economy** — creating a thread is cheaper than creating a process; context-switching is faster.",
    "• **Scalability** — true parallelism on multicore.",
    "## Concurrency vs Parallelism",
    "• **Concurrency** — multiple tasks make progress, possibly interleaved on one CPU.",
    "• **Parallelism** — multiple tasks literally run at the same instant on multiple cores.",
    "Single-core systems can be concurrent but not parallel. They are independent concepts.",
    "## Two types of parallelism",
    "• **Data parallelism** — same operation over different subsets of the data (4 cores summing quarters of an array).",
    "• **Task parallelism** — different operations on different cores (decode / render / network).",
    "## Multithreading models — user-thread ↔ kernel-thread mapping",
    "• **Many-to-one** — many user threads → one kernel thread. Library-only. No real parallelism on multicore. One blocking syscall stalls ALL threads.",
    "• **One-to-one** — each user thread = its own kernel thread. Real parallelism, no blocking issue. Cost: kernel threads are expensive; OS caps the count. Linux (NPTL), Windows.",
    "• **Many-to-many** — many user threads multiplexed over a smaller number of kernel threads via **LWPs** (lightweight processes). Best balance, complex.",
    "## Thread libraries",
    "• **POSIX Pthreads** — UNIX-like; can be user-level or kernel-level.",
    "• **Windows threads** — kernel-level.",
    "• **Java threads** — managed by the JVM, mapped to host threads underneath.",
    "## Implicit threading — let the runtime manage threads",
    "• **Thread pools** — pre-created workers waiting for tasks. No per-request creation cost; bounds concurrency.",
    "• **OpenMP** — compiler directives (#pragma omp parallel for) declaratively parallelize loops.",
    "• **Grand Central Dispatch (Apple)** — submit blocks to dispatch queues; system-managed pool.",
    "• **Fork-Join** — Java's ForkJoinPool. Recursive split-merge.",
    "## Threading issues",
    "• **fork() in a multithreaded process** — duplicate only the calling thread, or all threads? Most systems duplicate only the caller. exec() always replaces the whole image.",
    "• **Signal handling** — async signal goes to: the offending thread (SIGSEGV), any unmasked thread (SIGINT), a designated handler, or every thread.",
    "• **Thread cancellation** — async (immediate; risky — may hold locks) vs deferred (target checks cancellation points and exits cleanly).",
    "• **Thread-local storage (TLS)** — per-thread variables with global-like syntax. Used for errno, transaction state, per-thread caches.",
    "## Race conditions",
    "Bug whose outcome depends on the timing of concurrent accesses to shared state. Without synchronization, two threads can both read the same counter, increment, and both write +1 — one update lost. Fix with **mutex** (mutual exclusion) or **semaphore**.",
    "## Amdahl's law",
    "Maximum speedup = 1 / (s + p/N) where s = serial fraction, p = parallel fraction, N = cores. With 50% serial code on infinite cores: speedup ≤ 2. Limited by the inherently sequential part.",
    "## Linux specifics",
    "Linux treats threads and processes uniformly — all are 'tasks'. The **clone()** syscall is the underlying mechanism; flags decide what is shared. Pthreads runs over clone(); a thread = a process sharing memory with peers."
  ],
  summaryRu: [
    "**Поток** — базовая единица использования CPU. У каждого свой Thread ID, PC, регистры, стек. Потоки одного процесса ДЕЛЯТ его код, данные, кучу, ресурсы ОС (открытые файлы, сигналы, адресное пространство). Это и сила, и опасность.",
    "## Зачем нужны потоки",
    "Однопоточный процесс делает одно дело за раз. Текстовый редактор с одним потоком замораживает UI при проверке орфографии. Серверы (web, БД) — поток на запрос (или пул потоков) для параллельного обслуживания клиентов.",
    "## Четыре плюса многопоточности",
    "• **Отзывчивость** — UI интерактивен при фоновой работе.",
    "• **Разделение ресурсов** — потоки по умолчанию делят память процесса; гораздо дешевле IPC.",
    "• **Экономия** — создать поток дешевле, чем процесс; переключение быстрее.",
    "• **Масштабируемость** — реальный параллелизм на multicore.",
    "## Конкурентность vs Параллелизм",
    "• **Конкурентность** — несколько задач продвигаются, возможно, попеременно на одном CPU.",
    "• **Параллелизм** — реально выполняются одновременно на разных ядрах.",
    "Одноядерные могут быть конкурентны, но не параллельны. Концепции независимы.",
    "## Два вида параллелизма",
    "• **По данным** — одна операция на разных подмножествах данных (4 ядра суммируют четверти массива).",
    "• **По задачам** — разные операции на разных ядрах (декод / рендер / сеть).",
    "## Модели многопоточности — отображение user-thread ↔ kernel-thread",
    "• **Many-to-one** — много пользовательских → один ядерный. Только библиотека. Нет реального параллелизма на multicore. Один блокирующий syscall стопорит ВСЕ.",
    "• **One-to-one** — каждый пользовательский = свой ядерный. Реальный параллелизм, нет блокировок. Минус: ядерные потоки дороги; ОС ограничивает число. Linux (NPTL), Windows.",
    "• **Many-to-many** — много пользовательских мультиплексируются через меньшее число ядерных через **LWP** (lightweight processes). Лучший баланс, сложно.",
    "## Библиотеки потоков",
    "• **POSIX Pthreads** — UNIX-подобные; user-level или kernel-level.",
    "• **Windows threads** — kernel-level.",
    "• **Java threads** — управляются JVM, маппятся на потоки хоста.",
    "## Неявная многопоточность — потоки управляет рантайм",
    "• **Пулы потоков** — предсозданные рабочие, ждут задач. Нет затрат на создание; ограничение конкуренции.",
    "• **OpenMP** — директивы компилятору (#pragma omp parallel for) декларативно параллелизуют циклы.",
    "• **Grand Central Dispatch (Apple)** — блоки кода в dispatch-очереди; пул управляется системой.",
    "• **Fork-Join** — Java ForkJoinPool. Рекурсивное split-merge.",
    "## Проблемы многопоточности",
    "• **fork() в многопоточном процессе** — дублировать только вызывающий поток или все? Большинство систем — только вызывающий. exec() всегда заменяет весь образ.",
    "• **Сигналы** — async-сигнал идёт: потоку-виновнику (SIGSEGV), любому потоку без маски (SIGINT), назначенному обработчику, или всем.",
    "• **Отмена потока** — async (мгновенно; рискованно — мог держать локи) vs deferred (поток проверяет точки отмены и выходит чисто).",
    "• **Thread-local storage (TLS)** — пер-потоковые переменные с глобальным синтаксисом. Для errno, состояния транзакции, локальных кэшей.",
    "## Гонки данных (race conditions)",
    "Баг, исход которого зависит от тайминга конкурентных доступов к общему состоянию. Без синхронизации два потока могут оба прочитать счётчик, инкрементировать и оба записать +1 — одно обновление потеряется. Решается **мьютексом** (взаимное исключение) или **семафором**.",
    "## Закон Амдала",
    "Максимальное ускорение = 1 / (s + p/N), где s — серийная доля, p — параллельная, N — ядер. При 50% серийного кода на бесконечном числе ядер: speedup ≤ 2. Ограничено непараллельной частью.",
    "## Linux особенности",
    "Linux трактует потоки и процессы единообразно — это всё 'tasks'. Базовый механизм — syscall **clone()**, флаги задают, что разделять. Pthreads построен поверх clone(); поток = процесс, делящий память с другими."
  ],
  flashcards: [
    {q: "What does a thread own privately, and what does it share with its peers?", a: "Private: thread ID, program counter, registers, stack. Shared (with threads in the same process): code, data section, heap, open files, signals.", ru: "Личное: ID, PC, регистры, стек. Общее с потоками того же процесса: код, секция данных, куча, открытые файлы, сигналы.", diff: "easy"},
    {q: "List four benefits of multithreading.", a: "(1) Responsiveness — UI stays interactive. (2) Resource sharing — threads share memory natively. (3) Economy — creating a thread is cheaper than a process. (4) Scalability — uses multiple cores.", ru: "(1) Отзывчивость, (2) разделение ресурсов, (3) экономичность создания, (4) масштабируемость на многоядерных.", diff: "easy"},
    {q: "Concurrency vs parallelism.", a: "Concurrency: multiple threads make logical progress (possibly time-sliced on one CPU). Parallelism: multiple threads literally execute simultaneously on multiple cores. Single-core can have concurrency but not parallelism.", ru: "Конкурентность: потоки логически продвигаются (возможно, на одном CPU поочерёдно). Параллелизм: потоки реально выполняются одновременно на нескольких ядрах. На одноядерной — есть конкурентность, нет параллелизма.", diff: "medium"},
    {q: "Data parallelism vs task parallelism.", a: "Data parallelism splits the SAME operation across DIFFERENT data subsets (each core sums part of an array). Task parallelism distributes DIFFERENT operations across cores (one decodes, one renders, one networks).", ru: "Параллелизм по данным: одна операция на разных подмножествах данных. Параллелизм задач: разные операции на разных ядрах.", diff: "medium"},
    {q: "Many-to-one threading model — weakness.", a: "Many user threads mapped to a single kernel thread: cannot run in parallel on multiple cores, and one thread blocking on a syscall blocks ALL the threads.", ru: "Много пользовательских потоков → один поток ядра: нет реального параллелизма, и блокировка одного syscall'ом останавливает ВСЕ.", diff: "medium"},
    {q: "One-to-one threading model — trade-off.", a: "Each user thread maps to its own kernel thread → great parallelism and no blocking issue. Trade-off: creating a kernel thread for every user thread is expensive; OSes often limit thread count.", ru: "Каждый пользовательский поток — отдельный поток ядра: хороший параллелизм, нет блокировок. Минус: создание потоков ядра дорого; ОС ограничивает их число.", diff: "medium"},
    {q: "What is a thread pool and why use one?", a: "A pre-created collection of worker threads waiting for tasks. Benefits: avoids thread-creation cost per request, allows bounding the number of concurrent threads, smoother server performance.", ru: "Заранее созданный набор рабочих потоков, ожидающих задач. Плюсы: нет затрат на создание на каждый запрос, ограничение числа потоков, стабильная производительность сервера.", diff: "medium"},
    {q: "fork() in a multithreaded process — what semantics exist?", a: "Two semantics: (a) duplicate ONLY the calling thread in the child, or (b) duplicate ALL threads. Most modern UNIXes default to (a) — only the calling thread survives in the child.", ru: "Две семантики: (а) дублировать только вызывающий поток в дочернем процессе или (б) все потоки. Современные UNIX обычно используют (а).", diff: "hard"},
    {q: "Asynchronous vs deferred thread cancellation.", a: "Async: target thread is killed immediately — may leave shared data in inconsistent state. Deferred: target periodically checks a cancellation point and exits cleanly when safe. Deferred is preferred.", ru: "Асинхронная: поток убивается сразу — может оставить общие данные в плохом состоянии. Отложенная: поток проверяет точки отмены и выходит чисто. Предпочтительна отложенная.", diff: "hard"},
    {q: "What is thread-local storage (TLS)?", a: "Per-thread variables that look like globals but each thread has its own copy. Useful for per-thread state (errno in C, per-thread caches) without locking.", ru: "Переменные, видимые потоку как глобальные, но у каждого потока — своя копия. Полезно для состояния потока (errno в C, локальные кэши) без блокировок.", diff: "medium"},
    {q: "Where do signals get delivered in a multithreaded process?", a: "Options: to the thread that caused it (synchronous signal like SEGV), to a specific thread, to every thread, or to a designated handler thread. Asynchronous signals like SIGINT typically go to any one thread that hasn't masked them.", ru: "Варианты: потоку-виновнику (синхронные, например SEGV), конкретному потоку, всем потокам, или назначенному обработчику. Асинхронные сигналы (SIGINT) — любому потоку, не маскировавшему их.", diff: "hard"},
    {q: "What is a lightweight process (LWP)?", a: "An intermediate kernel-level object used by many-to-many models: user threads run on top of LWPs, which the kernel schedules. LWPs are the bridge between user threads and kernel CPUs.", ru: "Промежуточный объект ядра в модели many-to-many: пользовательские потоки выполняются поверх LWP, а ядро планирует LWP. Это мост между пользовательскими потоками и CPU.", diff: "hard"}
  ],
  quiz: [
    {q: "Which is shared between threads of the same process?", opts: ["Stack", "Program counter", "Heap and global data", "CPU registers"], correct: 2, expl: "Threads share the process's heap, code, data, and open files. Each thread has its own PC, registers, and stack.", explRu: "Потоки делят кучу, код, данные и открытые файлы процесса. PC, регистры и стек у каждого свои."},
    {q: "On a single-core CPU, which is possible?", opts: ["Parallelism only", "Concurrency only", "Both", "Neither"], correct: 1, expl: "A single core can only run one thread at an instant, but the OS can time-slice to achieve concurrency. Real parallelism requires multiple cores.", explRu: "Одно ядро выполняет один поток в момент времени, но ОС может разделять время — это конкурентность. Реальный параллелизм требует нескольких ядер."},
    {q: "Summing two arrays element-by-element across 4 cores by splitting the arrays is an example of:", opts: ["Task parallelism", "Data parallelism", "Implicit threading", "Pipeline parallelism"], correct: 1, expl: "Same operation, different subsets of data → data parallelism.", explRu: "Одна и та же операция над разными частями данных — параллелизм по данным."},
    {q: "Why might one-to-one threading be limited by the OS?", opts: ["It cannot run in parallel", "Each user thread requires a kernel thread, which is expensive; OS limits thread counts", "Kernel threads can't block", "It needs a virtual machine"], correct: 1, expl: "Allocating kernel resources per thread is expensive, so systems cap thread counts.", explRu: "Каждый пользовательский поток требует поток ядра, что дорого; ОС ограничивает их число."},
    {q: "Asynchronous thread cancellation is generally considered:", opts: ["Safer than deferred cancellation", "Risky because shared data may be left inconsistent", "Faster on multicore CPUs", "Only available in Java"], correct: 1, expl: "Asynchronous cancellation kills the target instantly — it may be holding locks or mid-update of shared state. Deferred (at safe points) is preferred.", explRu: "Асинхронная отмена убивает мгновенно — поток мог удерживать блокировки. Отложенная (в безопасных точках) безопаснее."},
    {q: "A thread pool is mainly used to:", opts: ["Encrypt thread communications", "Avoid thread-creation overhead and bound concurrency", "Implement many-to-one mapping", "Prevent context switches"], correct: 1, expl: "Pre-created workers wait for tasks, eliminating per-request thread creation cost and capping concurrent threads.", explRu: "Предсозданные рабочие ждут задач — экономия на создании и ограничение числа потоков."},
    {q: "Calling fork() in a multithreaded program typically results in:", opts: ["All threads being duplicated in the child", "Only the calling thread surviving in the child", "An automatic exec() call", "A kernel panic"], correct: 1, expl: "Most modern systems duplicate only the calling thread to avoid surprising lock and state issues in the child.", explRu: "Большинство современных систем дублируют только вызывающий поток, чтобы избежать проблем с блокировками и состоянием в дочернем."},
    {q: "Implicit threading (OpenMP, thread pools, GCD) shifts thread management from:", opts: ["The kernel to the hardware", "The programmer to the compiler/runtime/library", "The application to the user", "The library to the kernel"], correct: 1, expl: "Implicit threading moves thread creation and scheduling out of explicit programmer code into compilers, runtimes, or system libraries.", explRu: "Неявная многопоточность переносит создание и планирование потоков из явного кода в компилятор, рантайм или библиотеки системы."}
  ],
  traps: [
    {t: "'Concurrency = parallelism' is a CLASSIC trap. They are not the same: concurrency is about structure (multiple things in progress), parallelism is about execution (literally at the same time).", ru: "«Конкурентность = параллелизм» — классическая ловушка. Это не одно и то же: конкурентность — структура (много вещей в продвижении), параллелизм — исполнение (реально одновременно)."},
    {t: "Many-to-one is NOT good for blocking syscalls — if one user thread blocks on I/O, ALL threads stall, because they share one kernel thread.", ru: "Many-to-one плохо переносит блокирующие syscall: если один пользовательский поток заблокировался на I/O — ВСЕ потоки останавливаются, так как делят один поток ядра."},
    {t: "Threads share open files. If one thread closes an FD, the others lose it too. This is a common bug source in real code.", ru: "Потоки делят открытые файлы. Если один поток закрыл FD, другие тоже его теряют — типичный источник багов."}
  ]
};
